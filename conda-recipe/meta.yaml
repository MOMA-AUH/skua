{% set name = "skua" %}
{% set version = "0.3.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ..

build:
  number: 0
  noarch: python

requirements:
  host:
    - python >=3.11
    - pip
    - setuptools >=69
    - wheel
  run:
    - python >=3.11
    - pysam >=0.22

test:
  imports:
    - skua
  commands:
    - skua annotate --help

about:
  home: https://github.com/MOMA-AUH/skua
  summary: Predatory and kleptoparasitic variant caller for detecting subclonal mutations in deep sequencing data
  license: MIT
  license_file: LICENSE
